# ai-review 触发评审 · 工具链指令速查

> 本文只讲**怎么把评审跑起来**：命令、参数、环境变量、退出码、排障。
> 链路原理与报告页面架构见 `评审链路与说明.md`；CLI/平台架构约束见 `AGENTS.md`。
> 所有命令以 `d:\code\ai-flows`（工具本体）与 `d:\code\private\text-code`（目标仓库）为例，按需替换路径。

## 0. 工具链全景

```
                      ┌─ 入口 A：pre-push hook ── git push 被拦截 → 评审页面 → 点「确认提交」放行
git 变更 ── 触发 ─────┼─ 入口 B：手动 run ─────── 命令行直接评审（--push 才推送）
                      └─ 入口 C：平台节点 03 ─── 平台页面点「执行」→ 后台跑评审
                                   │
                      collectDiff → reviewBatch(LLM) → decideGate → 报告(md + JSON)
                                   │
                      退出码 0 通过 / 1 有 blocker / 2 推送失败
```

三个入口共用同一套评审链与同一份配置 `<目标仓库>/ai-review.config.json`。

## 1. 一次性准备（每个目标仓库只做一次）

```powershell
# ① 构建 ai-review（在工具仓库）
cd d:\code\ai-flows
npm install
npm run build                       # = tsc，产出 dist/；改完 src 必须重建

# ② 在目标仓库生成配置
cd d:\code\private\text-code
node d:/code/ai-flows/dist/index.js init      # 从 config.example.json 生成 ai-review.config.json
#   然后编辑：model.baseUrl / model.model / model.apiKeyEnv、severityBlocked、targets

# ③ 安装 pre-push hook
node d:/code/ai-flows/dist/index.js install-hook

# ④ 注入模型 Key（用户级持久化，setx 只对新开终端生效）
setx DEEPSEEK_API_KEY "sk-你的key"
```

> **必须用 `dist/` 装 hook**：hook 脚本里写死的是「安装时正在执行的那个入口文件」。
> 用 `npx tsx src/index.ts install-hook` 装，hook 里会变成 `node .../src/index.ts`，裸 node 跑不了 `.ts`，push 时静默失效。
> 已有 hook 会被自动备份为 `pre-push.bak-<时间戳>`。

## 2. 入口 A：pre-push hook（日常主路径）

```powershell
git add -A
git commit -m "feat: xxx"
git push origin ai-review-demo      # 被 hook 拦下 → 出评审页面链接
```

hook 实际执行：

```powershell
node d:/code/ai-flows/dist/index.js run --config ai-review.config.json --no-push --page
```

| 行为 | 说明 |
|---|---|
| 拦截策略 | **始终 `exit 1`**——不论评审是否通过，本次 `git push` 一律被拦，代码不出本地 |
| 放行方式 | 打开打印出的评审页面，输入 commit 信息，点「确认提交」按钮，由评审服务代为 `commit` + `push` |
| 防循环 | 服务内部推送时置 `AI_REVIEW_INTERNAL_PUSH=1`，hook 首行即 `exit 0` 跳过 |
| 前置条件 | 仓库根目录存在 `ai-review.config.json`，否则 hook 直接放行（不拦） |

页面「确认提交」背后的接口（`POST /reports/<id>/push`，body `{"message":"..."}`）：

| 前置校验 | 不满足时 |
|---|---|
| 报告存在 | 404 |
| `view.passed === true` | 400 `评审未通过，禁止推送` |
| 已配置 `targets` 且记录 `repoCwd` | 400 `未配置推送目标或仓库路径` |
| `message` 非空 | 400 `请输入 commit 提交信息后再提交` |

通过校验后的提交策略：有暂存变更 → `commitStaged(message)` 再推送；无暂存但 `message` 与 HEAD 不同 → `amendCommitMessage(message)` 改写最近一次提交；否则直接推送。

## 3. 入口 B：手动 run

```powershell
# 仅评审 + 出页面，不推送（推荐先跑这个看结果）
node d:/code/ai-flows/dist/index.js run --config ai-review.config.json --no-push --page

# 评审通过后由命令行直接推送（跳过页面确认）
node d:/code/ai-flows/dist/index.js run --config ai-review.config.json --push

# 开发态直跑 TS 源码（等价于 npm run dev）
npx tsx d:/code/ai-flows/src/index.ts run --config ai-review.config.json --page
```

`run` 参数：

| 参数 | 作用 |
|---|---|
| `--config <path>` | 指定配置文件；缺省按「CLI > `AI_REVIEW_CONFIG` > `./ai-review.config.json`」解析 |
| `--page` | 评审后自动后台拉起报告服务并打印可点开链接 |
| `--push` / `--no-push` | 命令行直接推送 / 强制不推送；**缺省不推送**，`--no-push` 优先 |
| `--report <path>` | 指定 Markdown 报告输出路径（缺省 `review-report.md`） |

## 4. 入口 C：平台节点 03

```powershell
node d:/code/ai-flows/dist/index.js platform --port 4311 --repo d:/code/private/text-code
```

- 登录页 `/login`，管线页 `/pipeline`；账号见 `db/users.json`，演示密码统一 `123456`。
- 节点 03（AI 代码评审）的「执行」按钮在**后台**跑真实评审链，目标仓库由 `--repo` 指定（缺省当前目录）。
- 评审配置随目标仓库走（`<repo>/ai-review.config.json`），平台自身不存额外评审配置；端口用 `AI_FLOWS_PORT` 覆盖。

## 5. 报告服务与页面路由

```powershell
# 常驻服务（run --page 会自动拉起，通常无需手动执行）
node d:/code/ai-flows/dist/index.js serve --port 4310
```

| 路由 | 说明 |
|---|---|
| `GET /` | 报告列表索引（PASS/BLOCK、时间、各级别计数，按时间倒序） |
| `GET /reports/<id>` | 读 `.ai-review-reports/<id>.json` → 动态渲染页面（id 限 `[^/\\]+`，防路径穿越） |
| `GET /health` | 返回 `ok`，供握手校验 |
| `POST /reports/<id>/push` | 页面「确认提交」按钮调用 |

- 报告只落盘 **JSON**（`.ai-review-reports/<id>.json`），HTML 由服务按需渲染，不落盘。
- 端口默认 4310（`AI_REVIEW_PORT`），占用时自动回退 `base ~ base+9`；真实端口记录在 `.ai-review-reports/.server`（`{port,url,dir}`）。
- 握手校验：`.server` 的 `dir` 必须等于当前仓库报告目录，且 `/health` 返回 `ok`，才复用该实例；否则新起一个并覆盖 `.server`。
- 生成物不入库：目标仓库 `.gitignore` 需忽略 `.ai-review-reports/` 与 `review-report.md`。

## 6. 配置速查（`<目标仓库>/ai-review.config.json`）

```json
{
  "diff": {
    "scope": "range",
    "base": "HEAD~1",
    "exclude": ["*.md", "*.json", "*.lock", "node_modules/**", ".claude/**"],
    "maxFileLines": 800
  },
  "model": {
    "baseUrl": "https://api.deepseek.com",
    "apiKeyEnv": "DEEPSEEK_API_KEY",
    "model": "deepseek-chat",
    "timeoutMs": 120000
  },
  "severityBlocked": ["blocker"],
  "targets": [
    { "name": "github-text-code", "remoteName": "origin", "branch": "ai-review-demo", "auth": "default" }
  ]
}
```

**`diff.scope` 决定「评什么」，是最容易踩坑的字段**（`src/git.ts` `scopeArgs`）：

| 取值 | 等价 git 命令 | 评审范围 |
|---|---|---|
| `staged` | `git diff --cached` | 暂存区（`git add` 后、`commit` 前） |
| `range` | `git diff <base>...HEAD` | 提交区间，如 `base=HEAD~1` 即**只评审最近一次提交** |
| 其他任意值（如 `working`） | `git diff` | 工作区未暂存变更 |

其余字段：

| 字段 | 说明 |
|---|---|
| `diff.exclude` | 排除 glob（`*` `**` `?`）；以 `/` 结尾按路径前缀匹配 |
| `diff.maxFileLines` | 单文件变更行数超此值走降级，只保留最严重一条问题 |
| `severityBlocked` | 命中即判定不通过，缺省 `["blocker"]` |
| `targets[]` | 推送目标：`remoteName`（远端名）或 `remoteUrl`；`tokenEnv` 指定 https token 环境变量 |
| `model.apiKeyEnv` | 密钥只从环境变量读，**禁止写进配置文件** |

## 7. 环境变量与退出码

| 环境变量 | 用途 |
|---|---|
| `DEEPSEEK_API_KEY` | 模型 Key（由 `model.apiKeyEnv` 指定，名可改） |
| `AI_REVIEW_CONFIG` | 配置文件路径（被 `--config` 覆盖） |
| `AI_REVIEW_PORT` | 报告服务端口（缺省 4310） |
| `AI_REVIEW_GITHUB_TOKEN` | https 目标远端 token 认证 |
| `AI_FLOWS_PORT` | 平台服务端口（缺省 4311） |
| `AI_REVIEW_INTERNAL_PUSH` | 置 `1` 时 hook 直接放行，仅供服务内部推送使用 |

| 退出码 | 含义 | 对 hook 的影响 |
|---|---|---|
| `0` | 评审通过 | 不影响门禁判断（hook 仍拦截，需页面确认提交） |
| `1` | 存在 blocker / 执行出错 | 拦截 |
| `2` | 推送失败 | — |

临时只给当前终端注入 Key：

```powershell
$env:DEEPSEEK_API_KEY='sk-你的key'; git push origin ai-review-demo
```

## 8. 只想评审暂存区（不改 `scope`、不动提交历史）

配置为 `scope=range` 时，暂存区变更不在评审范围内。临时验证可另存一份配置：

```powershell
# 1) 复制仓库配置为临时配置，仅把 diff.scope 改成 "staged"（删掉 base）
# 2) 暂存要评审的文件
git add tests/test_info.py tests/test_warning.py tests/test_blocker.py

# 3) 用临时配置跑评审（cwd 必须在目标仓库，配置里的相对路径才正确）
npx tsx d:/code/ai-flows/src/index.ts run --config <临时配置路径> --page
```

## 9. 排障速查

```powershell
# 密钥是否已注入当前会话
$env:DEEPSEEK_API_KEY

# 确认 hook 已装、内容指向 dist
Get-Content .git/hooks/pre-push

# 被拦下的提交还留在本地（未推到远端）
git log origin/ai-review-demo..ai-review-demo --oneline

# HTTPS 443 不通时切 SSH
ssh -T git@github.com
git remote set-url origin git@github.com:LavaTap/text-code.git

# SSH 22 端口被网络拦截时（报 Connection refused），改走官方 443 通道
# 写入 ~/.ssh/config：
#   Host github.com
#     HostName ssh.github.com
#     Port 443
#     User git
ssh -T git@github.com          # 应返回 Hi <用户名>! You've successfully authenticated
git ls-remote origin HEAD      # 验证 git 传输层可用

# 改完 ai-flows 源码后必须重建 dist，否则 hook 仍在跑旧产物
cd d:\code\ai-flows; npm run build
```

| 现象 | 原因 / 处理 |
|---|---|
| push 没被拦、也没评审 | 目标仓库根目录缺 `ai-review.config.json`，或 hook 里指向 `src/index.ts`（改用 `dist/` 重装） |
| `ssh: connect to host github.com port 22: Connection refused` | 网络拦截 22 端口。**此时 hook 还没执行**——git 先连远端、再跑 pre-push hook，所以报错里看不到任何评审输出。按上一节改用 443 通道即可 |
| 报告页面排版/行为是旧版 | `dist/` 未重建，hook 跑的是旧构建产物 |
| 提示「没有检测到任何代码变更」 | 当前 `scope` 下 diff 为空；核对是 `staged` 还是 `range` |
| 提示「变更文件中没有需要评审的代码」 | 变更全被 `diff.exclude` 过滤 |
| 页面「确认提交」400 | 未通过门禁 / 未配 `targets` / `message` 为空，按返回文案对应处理 |
| 端口不是预期的 4310 | 被占用已回退，实际端口见 `.ai-review-reports/.server` |

---

**红线：** 密钥只从环境变量读 · `run` 缺省不推送 · 确认提交必须带非空 commit message · 退出码 0/1/2 契约不可改 · 改完 `src` 必须重建 `dist` 再装/跑 hook
