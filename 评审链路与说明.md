# ai-review 评审链路与说明

> 本文档说明 `ai-review`（`d:\code\ai-flows`）的评审链路，以及如何在 `text-code` 仓库用**多等级测试用例**实测整个闭环。

## 1. 核心流程（评审链路）

```
本地编码 ──git add/commit──> pre-push hook 触发 ai-review
                                   │
                                   ├─ 采集 git diff（scope=range/HEAD~1）
                                   ├─ 本地调用 LLM（DeepSeek）评审，输出结构化 JSON
                                   ├─ 门禁判定：命中 severityBlocked=["blocker"] 则拦截
                                   ├─ 生成评审报告（md + HTML 页面）
                                   └─ 退出码：
                                         0 = 通过 ─> git push 继续，推到远端
                                         1 = 有 blocker ─> git push 被拦截（代码推不出去）
```

- 位置无关：采集、评审、门禁全在本地；`git push` 目标可以是 GitHub/Gitee/内部 GitLab/本地裸仓库（`targets` 里 `remoteUrl` 或 `remoteName` 任意指定）。
## 2. 评审网页

### 2.1 页面形态
- 每次评审通过 `--page` **只把评审结果以 JSON 数据落盘**（`.ai-review-reports/<id>.json`），页面由常驻报告服务读取该 JSON **按需动态渲染**——平台不再每次预生成一个 HTML 文件。
- 渲染出的页面为自包含 HTML（CSS 内联、无外部依赖）：顶栏品牌 + 面包屑、评审结论 Hero、摘要、6 张指标卡、问题卡片列表、页脚。
- 采用参考模板（Vela 暖暗黑风）。
- 未通过时 Hero 自动切换为**红色 BLOCK 态**（X 图标 + BLOCK 徽标 + 拦截条数），通过时为绿色 PASS 态。
- 问题正文/建议中的 `` `反引号` `` 片段会自动渲染成 `<code>` 行内代码。

### 2.2 数据流：AI JSON → 视图模型 → JSON 落盘 → 服务动态渲染
```
AI 返回 JSON ──normalizeIssues──> ReviewResult
                                      │
                    formatReport(result, gate, meta)   ← reviewer.ts【格式化接口】
                                      ▼
                                 ReportView（视图模型）
                                      │
                    writeFileSync → .ai-review-reports/<id>.json   ← 只存数据，不存 HTML
                                      │
                    serve.ts: 读 <id>.json → JSON.parse → renderTemplate(view)
                                      ▼
                          动态渲染 HTML 返回浏览器（不落盘）
```

**① 格式化接口（`src/reviewer.ts`）**
- `formatReport(result, gate, meta)`：把 AI 返回的 `ReviewResult` + 门禁判定整理成 `ReportView`，是 reviewer 对外暴露的**唯一格式化入口**。
- `ReportView` 字段：`passed / generatedAt / repo / ref / filesReviewed / degradedCount / total / counts / categories / summary / issues[] / pushes?`。
- `meta` 可传 `{ repo, ref, generatedAt, pushes }`；`issues[]` 已把 `file + line` 预拼成 `loc`（"文件:行号"）。
- 好处：渲染层只消费视图模型，不关心 AI 原始 JSON 结构；换模板不动评审逻辑，换模型不动渲染逻辑。
- **脱敏**：`reviewBatch` 在产出结果前用 `src/redact.ts` 的 `maskSecrets()` 对 `summary` 与每条 `message/suggestion` 打码（密钥/令牌只保留首尾各 4 位），避免报告二次泄露被评审代码里的凭据。
- **摘要去重**：`buildSummary()` 按描述去重，同描述的文件合并为一行，避免批量生成物产生大段重复摘要。

**② 模板渲染（`src/reporter.ts`）**
- `buildReportView(result, gate, pushes?, meta?)` = `formatReport(...)`，产出待落盘的 `ReportView`。
- `renderTemplate(view)` 用模板字符串把视图模型注入参考模板，**已导出**供 `serve.ts` 按需渲染；`esc()` 做 HTML 转义、`rich()` 额外把反引号转 `<code>`，防 XSS/破版。
- `REPORT_CSS` 内联参考模板样式，并补齐了未通过态（`.verdict.block`）与推送区（`.pushes`）的样式。

**③ HTTP 服务（`src/serve.ts`）** 路由：
  | 路径 | 说明 |
  |---|---|
  | `GET /reports/<id>` | 读取 `<仓库>/.ai-review-reports/<id>.json` → `JSON.parse` → `renderTemplate(view)` **动态渲染**页面（正则限定 `[^/\\]`，防路径穿越） |
  | `GET /` | 报告列表索引：遍历目录下 `.json`，读出 `PASS/BLOCK`、时间、各级别计数并按时间倒序展示 |
  | `GET /health` | 返回 `ok`，与 `.server` 握手文件里的 `dir` 字段一起校验：`dir` 必须等于当前仓库报告目录、且 `/health` 返回 `ok`，才视为本仓库的可用服务，避免同端口别的 ai-review 实例被误信 |

### 2.3 其他
- 端口：默认 4310（`AI_REVIEW_PORT`），被占用时自动回退尝试 base~base+9；真实端口通过 `.ai-review-reports/.server` 握手文件记录。握手同时校验 `dir` 字段（= 当前仓库报告目录）与 `/health`（返回体必须为 `ok`）——两者都满足才算可信，避免陈旧 `.server` 指向同端口别的 ai-review 实例（`dir` 不匹配）而被误信导致 404。
- 面包屑 `repo / ref / report`：`ref` 取当前分支（`currentBranch`），缺失项自动省略。
- 页面链接示例：`http://127.0.0.1:4312/reports/review-mue9u338`
- 服务是常驻后台进程，只监听 127.0.0.1。两种起法：
  - **`--page` 自动拉起**：`run --page` 完成评审、落盘 JSON 后，自动 detached spawn 一个属于**本仓库目录**的 serve 子进程并打印可点开的链接（无需手动 `serve`）。spawn 时复用 `process.execArgv`，把 tsx 的 `--require`/`--import` loader 一并传给子进程；`tsx src/index.ts` 运行态下这样子进程才能解析 `.ts` 源码（否则裸 node 跑 `.ts` 会 `ERR_MODULE_NOT_FOUND` 崩溃、`.server` 写不出）。dist 构建产物（`.js`）场景下 `execArgv` 为空，由 node 直接执行。
  - **手动常驻**：`ai-review serve --port <n>`（见 §5.5），适合长期查看历史报告。
  - 两者通过 `.server` 握手文件复用同一实例：`dir` + `/health` 双校验通过即复用，否则新起一个并覆盖 `.server`。
  - （团队共享场景可另行把该目录放到公网托管，见第 6 节建议。）
- **生成物不入库**：`.ai-review-reports/` 与 `review-report.md` 均为工具生成的产物，应在目标仓库 `.gitignore` 中忽略，只保留数据（JSON）供服务渲染。

## 3. 在 text-code 上实测的三档用例

| 级别 | 文件 | 代码特征 | 评审结果 | 推送结果 |
|---|---|---|---|---|
| info | `tests/test_info.py` | 冗余封装 `_wrap_len` 透传 `len()` | 3×info，通过，退出码 0 | ✅ 已推送 `b285ce9` |
| warning | `tests/test_warning.py` | key 未校验、文件句柄未关闭 | 3×warning + 1×info，通过 | ✅ 已推送 `627f519` |
| blocker | `tests/test_blocker.py` | 源码硬编码真实 API 密钥 | 2×blocker，拦截，退出码 1 | ⛔ 拦截演示；后改为 gitignore 忽略，未入库 |

### 实测结论
- info / warning → 门禁通过 → 自动放行 push 到 `origin/ai-review-demo`。
- blocker（硬编码密钥）→ 门禁拦截 → `git push` 失败，代码**没有**进入远程仓库，实现"坏代码推不出去"。
- **评审由 LLM 判定**：同是 SQL 拼接，可能会被拔高为 blocker（首次 warning 用例因 SQL 注入被判 2×blocker 被拦）。造用例需谨慎。
- **含密钥用例的最终处置**：仅加 `.gitignore` 不足以消除泄露（密钥已在提交里），需配合 `git rm --cached <file>` + `git commit --amend` 把文件从**提交历史**中摘除（文件仍保留在工作区），再 push。之后评审通过、远端分支不含该文件。

### 3.1 参考模板端到端验证

改用参考模板（Vela 暖暗黑）后，两态均以**真实 LLM 评审**验证通过：

| 验证态 | 触发方式 | 评审结果 | 页面确认 |
|---|---|---|---|
| BLOCK | 真实 `git push`（HEAD=blocker 提交） | 2×blocker，退出码 1，push 被拦截 | 红色 BLOCK Hero + X 图标，CSS/指标卡/页脚均正确 |
| PASS | 临时分支手动 `run --no-push --page` | 3×warning + 2×info，退出码 0 | 绿色 PASS Hero + ✔ 图标，徽标 `PASS` |

> PASS 态刻意**不走真实 push**：当前分支 HEAD 是含真实密钥的 blocker 提交，一旦放行会把密钥一起推到 GitHub。故改用临时分支 + `--no-push` 只验证页面渲染，避免密钥外泄。

## 4. 关键节点的产生过程

### 4.1 认证切换（HTTPS → SSH）
- `https://github.com/...` 的 443 端口在本机连不上，而 SSH 通道可用（`ssh -T git@github.com` 返回 `Hi LavaTap!`）。
- 将 `origin` 从 HTTPS 切到 SSH：
  ```powershell
  git remote set-url origin git@github.com:LavaTap/text-code.git
  ```

### 4.2 配置文件 `ai-review.config.json`
```json
{
  "diff": { "scope": "range", "base": "HEAD~1", "exclude": ["*.md","*.json","*.lock","node_modules/**",".claude/**"], "maxFileLines": 800 },
  "model": { "baseUrl": "https://api.deepseek.com", "apiKeyEnv": "DEEPSEEK_API_KEY", "model": "deepseek-chat", "timeoutMs": 120000 },
  "severityBlocked": ["blocker"],
  "targets": [ { "name": "github-text-code", "remoteName": "origin", "branch": "ai-review-demo", "auth": "default" } ]
}
```

### 4.3 安装 pre-push hook
```powershell
node d:/code/ai-flows/dist/index.js install-hook
# → 写入 .git/hooks/pre-push，要求仓库根目录存在 ai-review.config.json
```

### 4.4 API Key 注入
- hook 从 `git push` 进程继承环境变量，读取 `model.apiKeyEnv` 指定的 `DEEPSEEK_API_KEY`。
- 持久化（用户级，需自行执行，不在仓库内写死明文）：
  ```powershell
  setx DEEPSEEK_API_KEY "sk-xxx"
  ```
  > 注意：`setx` 只对**新开**的终端生效；优先从资源管理器/开始菜单新开 PowerShell。

## 5. 触发评审全流程指令

### 5.1 一次性准备（每个仓库只做一次）

```powershell
# 1) 编译 ai-review（在 ai-flows 目录）
cd d:\code\ai-flows
npm install
npx tsc                                   # 产出 dist/

# 2) 在目标仓库根目录生成配置
cd d:\code\private\text-code
node d:/code/ai-flows/dist/index.js init  # 生成 ai-review.config.json
#   编辑 ai-review.config.json：填 model（baseUrl/model/apiKeyEnv）、
#   severityBlocked、targets（name/remoteName/branch）

# 3) 安装 pre-push hook（在目标仓库根目录）
node d:/code/ai-flows/dist/index.js install-hook

# 4) 配置模型 API Key（用户级持久化，需新开终端才生效）
setx DEEPSEEK_API_KEY "sk-你的key"
```

### 5.2 日常触发（核心三步）

```powershell
# ① 暂存并提交
git add -A
git commit -m "feat: xxx"

# ② 推送（自动触发评审）
git push origin <branch>
```

`git push` 前 hook 自动执行 `run --config ai-review.config.json --no-push --page`：
采集 diff → AI 评审 → 门禁判定 → 生成 md + HTML 报告 → 打印页面链接，然后按退出码决定放行：

| 评审结果 | hook 退出码 | push 行为 |
|---|---|---|
| 无 blocker | 0 | **放行**，代码进入远端 |
| 有 blocker | 1 | **拦截**，代码推不出去，仅留本地 |

### 5.3 临时（仅当前终端）注入 Key

不想 `setx` 时，可只给本次命令带上：

```powershell
$env:DEEPSEEK_API_KEY='sk-你的key'; git push origin <branch>
```

### 5.4 手动跑评审（不经过 hook、不推送）

```powershell
node d:/code/ai-flows/dist/index.js run --config ai-review.config.json --no-push --page
```

- 评审通过后想自动推送到目标远端：把 `--no-push` 换成 `--push`
- 不需要页面：去掉 `--page`
- 指定报告输出路径：加 `--report <path>`

### 5.5 报告服务

```powershell
node d:/code/ai-flows/dist/index.js serve --port 4310   # 常驻；端口占用自动回退
# 浏览器打开 http://127.0.0.1:4310/  查看全部历史报告
```

> `run --page` 也会自动后台 detached 拉起一个属于本仓库的 serve 子进程并打印可点开的链接（无需手动开 `serve`）；手动 `serve` 适合长期常驻查看历史报告。两者经 `.server` 握手文件复用同一实例：`dir` + `/health` 双校验通过即复用，否则新起一个并覆盖 `.server`。

### 5.6 命令速查 / 排障

```powershell
# 确认 SSH 认证（HTTPS 的 443 不通时改用 SSH）
ssh -T git@github.com
git remote set-url origin git@github.com:LavaTap/text-code.git

# 查看尚未推送到远端的提交（被 block 的会一直留在这里）
git log origin/<branch>..<branch> --oneline

# 强制放行（不推荐，绕过门禁就失去意义了）
# git push --force origin <branch>
```

## 6. 接入「评审标准 Skill」的计划与建议

> 现状：ai-flows 的评审 prompt（`src/reviewer.ts` 的 `buildPrompt`）把「评审维度 + 严重级别定义」**内置写死**在源码里，
> 不支持按团队 Code 规范定制。本节约接入第三方 Code 规范（用户将提供相应 md）的思路，让评审遵循具体规范。

### 6.1 目标
把外部的 Code 规范 md（命名、风格、安全红线、性能、可维护性约定等）作为**评审标准 skill** 注入到评审 prompt，
使 ai-review 输出符合该规范，而不是仅靠通用经验。

### 6.2 可选接入方案（从轻到重）

| 方案 | 做法 | 改动量 | 优点 | 缺点 |
|---|---|---|---|---|
| A. 配置注入（推荐起步） | config 增加 `model.standardFiles?: string[]`，`buildPrompt` 读取这些 md 拼进 prompt 前部 | 小（改 reviewer/collector + config） | 无需中台，规范即文件 | 文件大时挤占 token |
| B. Rules 目录 | 仓库约定 `ai-review-rules/*.md`，hook 前自动 glob 加载 | 中 | 规范随仓库版本化，自动生效 | 需处理加载/优先级 |
| C. Skill 插件 | 参照本认知的「Skill 机制」，把规范封装成独立 skill，由 skill-creator 管理 | 大 | 可复用、可分享 | 需工具链支持 skill 加载 |

### 6.3 建议顺序
1. 先走**方案 A**：用户提供 Code 规范 md 后，落盘到 `text-code`（如 `docs/code-standard.md`），
   在 `ai-review.config.json` 里加 `model.standardFiles: ["docs/code-standard.md"]`；
   我据此改 `reviewer.ts`，把规范内容插到 prompt 的「评审维度」之前。
2. 验证：用三档用例重跑，确认 warning 档能命中规范里更细的规则（比通用维度更贴合团队约定）。
3. 效果稳定后再评估是否升级方案 B/C。

### 6.4 规范注入点的约定（供后续实现参考）
- 规范片段插入位置：位于「评审维度」之前，作为「评审必须遵循的组织规范」约束；
- 冲突处理：规范条目 > 内置维度；同一问题若同时命中规范与通用维度，规格按规范界定级别；
- token 控制：`maxFileLines` 降级逻辑不变；若规范过大可先总结、只注入关键约束。
- 待办：由你提供 Code 规范 md 后，本文件会新增一节「实际接入的规范要点」。